/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "res.cloudinary.com",
            'play.google.com',
            'developer.apple.com',
        ],
    },
};

export default nextConfig;
